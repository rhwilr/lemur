function fibonacci (x) {
  if (x == 0) {
    return 0;
	}
	
	if (x == 1) {
		return 1;
	}

	// Printing a progress bar,
	if (x == 25) {
		print("■");
	}
	
	return fibonacci(x - 1) + fibonacci(x - 2);
};

println("Welcome to the Lemur benchmark.");
println("Running benchmark for Lemur engine: " + env("LEMUR_RUNTIME"));

println();
println("|___________________ Progress __________________________________________________________|");

fibonacci(35);
println();
println();