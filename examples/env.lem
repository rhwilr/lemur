// env returns a hash with all environment variables
let all = env();

// the index operator can be used to extract one specific value
print("Hi", all["USER"]);


// we can also pass the name of a variable directly to the env call
println();
println();

println("This script is executed using the '" + env("LEMUR_RUNTIME") + "' engine");